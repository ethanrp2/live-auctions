# Session 2026-04-22

## Did
- **M3 complete.** Live buyer screen shipped end-to-end:
  - [`lib/basta/ws.ts`](../../../lib/basta/ws.ts) — graphql-ws singleton wrapper to `NEXT_PUBLIC_BASTA_WS_URL` with bidder-token refresh per `connection_init`, auto-reconnect (`retryAttempts: Infinity`, `lazy: true`), typed helpers `subscribeItemUpdates` + `subscribeSaleUpdates`.
  - [`lib/hooks/use-sale-activity.ts`](../../../lib/hooks/use-sale-activity.ts) — subscribes to Basta `saleUpdates(saleId)`; returns `{ updates: Map<itemId, SaleUpdatePayload>, isConnected, error }`.
  - [`lib/hooks/use-bid-feed.ts`](../../../lib/hooks/use-bid-feed.ts) — Supabase Realtime on `bids` filtered by `lot_id=eq.${lotId}`, seeded with 50 newest + hydrated with `profiles.display_name` (fallback `"Anonymous"`).
  - [`app/auctions/[auctionId]/live/page.tsx`](../../../app/auctions/[auctionId]/live/page.tsx) — server component, tenant-guarded load of auction + lots with `current_lot_id` fallback to first non-closed lot.
  - [`app/auctions/[auctionId]/live/view.tsx`](../../../app/auctions/[auctionId]/live/view.tsx) — client `LiveAuctionView` wiring top bar, lot ribbon, hero, bid history, bid footer, custom + max sheets. `iAmLeader` derived from `bids[0].userId === user.id`. Unauthenticated taps open `<AuthModal>`.
- Memory docs updated: `basta-integration.md` ticked NORMAL + `saleUpdates`/`itemUpdates` checkboxes, added "Live buyer screen (M3)" subsection, bumped `Last verified` to 2026-04-22. `log.md` appended. This session file created. `INDEX.md` updated.

## Works
- `pnpm typecheck` clean (frontend workspace).
- Both real-time streams independently connected: Basta WS for price/countdown/status; Supabase Realtime for bid feed.
- `<AuthModal>` opens when an unauthenticated user taps BID.

## Broken / unverified
- No Figma visual parity pass — layout matches spec functionally but hasn't been diffed against frame 3705:*.
- No browser smoke test against a live OPEN sale this session; NORMAL `bidOnItem` round-trip not observed end-to-end.
- `resolveIncrement` edge cases still untested.
- `myBidStatus` not consumed — we derive winning/outbid from the bid feed, which is eventually-consistent relative to Basta's WS state. Good enough for v1.
- Three pending manual actions from prior sessions still open: (1) M1 browser MAX-bid test, (2) M2 ngrok live-fire, (3) Basta support email.

## Stopped at
- M3 shipped + memory updated. Awaiting commit + browser smoke test.

## Next (first action of next session)
1. Open [SCHEMA.md](../SCHEMA.md), then this file.
2. Boot `pnpm dev:all`, hit `basa.localhost:3000/auctions/<basa-auction-id>/live` as a signed-in buyer. Confirm the full live screen renders and a NORMAL bid round-trips. Watch devtools → Network → WS for `saleUpdates` frames.
3. Resurface the three deferred manual actions.
4. Start **M4** — seller console. Figma frames for the seller live dashboard: lot queue sidebar, lot media center, seller control panel (bid display, increment selector, SELL/PASS/NEXT LOT), buyer questions feed, live bid history. Backend will need `sale(id)` + `item(saleId, itemId)` Management API queries (to fetch current leader on SELL).
