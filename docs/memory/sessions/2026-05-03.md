# Session 2026-05-03

## Did
- **Branch merge:** Fast-forward merged `claude/frosty-feynman-8fd2b6` → `claude/festive-curie-b3a813`. All M0–M3 work (72 files) is now in the current working branch.
- **Doc consolidation:**
  - Wrote [`LIVE_AUCTIONS_PLAN.md`](../../../LIVE_AUCTIONS_PLAN.md) — unified product + architecture reference replacing the fragmented `LIVE_AUCTIONS_PLATFORM_OVERVIEW.md`, `LIVE_AUCTIONS_IMPLEMENTATION_GUIDE.md`, and `GAP_ANALYSIS_AND_PLAN.md`.
  - Wrote [`WORKING_NOTES.md`](../../../WORKING_NOTES.md) — agent-agnostic progress tracker with milestone checklist, pending actions, open questions, session log. Designed to be readable by any AI agent (Claude, Codex, etc.).
  - Updated [`AGENTS.md`](../../../AGENTS.md) — now references `LIVE_AUCTIONS_PLAN.md` + `WORKING_NOTES.md` correctly (previously had a broken reference to `LIVE_AUCTIONS_IMPLEMENTATION_PLAN.md` which never existed).
- **ADR-003:** Shipping is record-keeping only for MVP (no Shippo). Sellers view winner info in the console. Decision recorded in [`decisions/ADR-003-shipping-record-only.md`](../decisions/ADR-003-shipping-record-only.md).
- **Memory INDEX updated** to include ADR-003 and this session.
- **Verified tools:** Figma MCP + Supabase MCP both accessible. DB schema confirmed (10 tables present, all with expected columns and RLS).

## Current state
- Branch: `claude/festive-curie-b3a813`
- Milestones: M0–M3 complete, M4 starting.
- Typecheck: not run this session (no code changes). Last known clean state: end of M3 session.

## Next (first actions of next session)
1. Read this file, then `WORKING_NOTES.md`.
2. Pull the Figma seller console frames (desktop) via Figma MCP.
3. Build M4 seller console in parallel: backend routes (current-lot, sell, pass, end, winners) + frontend console layout + control panel components.
4. Apply any needed Supabase migrations via Supabase MCP.

## M4 implementation notes (for the implementing agent)
- Route: `app/console/[auctionId]/page.tsx` — seller-only, guard with `is_seller` check on the profile.
- The console is **desktop-only** — there are no mobile Figma frames for the seller console.
- Basta WS subscriptions are already wired in `lib/basta/ws.ts` — reuse `subscribeItemUpdates` / `subscribeSaleUpdates`.
- Supabase Realtime channels for `auction_questions` and `bids` are already in the publication.
- `auctions.current_lot_id` is the active lot pointer. NEXT LOT updates this and broadcasts via Supabase Realtime so buyers' lot ribbons update.
- SELL: creates an `orders` row + stamps `lots.winner_user_id`, `lots.winning_bid_cents`, `lots.sold_at`, `lots.live_status = 'sold'`. Must fetch the current leader's `amount_cents` from Basta before creating the order (use Management API `sale(id)` or derive from `bids` table sorted by `amount_cents DESC`).
- PASS: stamps `lots.live_status = 'passed'`.
- Winner info for seller view: join `orders` → `profiles` (display_name, shipping_address) + `auth.users` (email) — use service role for the email join.
