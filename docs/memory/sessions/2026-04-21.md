# Session 2026-04-21

## Did
- Wrote [GAP_ANALYSIS_AND_PLAN.md](../../../GAP_ANALYSIS_AND_PLAN.md) (full gap audit + Basta Q&A deep-dive).
- Wrote [`fancy-knitting-bunny.md`](../../../.claude/plans/fancy-knitting-bunny.md) — 8 demo-gated milestones (M0–M8). User approved.
- Seeded `docs/memory/` (this directory) per Karpathy LLM-wiki pattern. 13 files: SCHEMA, INDEX, log, 6 architecture pages, 2 ADRs, 3 risks, 1 glossary, this session.
- **M0 shipped:**
  - Migration `step12_money_units_cleanup` applied (audit passed; column `lots.money_in_cents` dropped).
  - `lib/format.ts` rewritten: `formatMoneyCents` / `formatEstimateCents` / `parseDollarsToCents`. All 3 callers updated.
  - Mock data in `lib/storefront-data.ts` converted to cents (literals × 100).
  - Seed scripts fixed — both now convert dollars→cents at DB boundary.
  - `.env.example` + `backend/.env.example` written with per-var notes and future-milestone slots.
  - `scripts/bootstrap.sh` + `pnpm bootstrap` + `pnpm typecheck` added.
  - Frontend + backend typecheck clean.

## Works
- `pnpm typecheck` (both workspaces).
- Supabase schema reflects ADR-002 (`money_in_cents` column gone; 24 lot rows preserved; values already in cents).
- Memory system self-consistent per [SCHEMA.md](../SCHEMA.md) rules: INDEX complete, every page has `Last verified`, every risk has trigger + owner.

## Broken / unverified
- Have not booted `pnpm dev:all` in this session (no `.env.local` filled; that's intentional — it's the fresh-clone demo gate). Do this early next session on a clean env to verify M0 actually lights up `basa.localhost:3000`.
- Not committed. Stage + commit before M1 to keep code+memory change together (SCHEMA rule 10).
- `.env.example` references `<your-project-ref>` placeholder; bootstrap script warns on it. Real values for this worktree: `https://fkatfnvscuvfejhdblks.supabase.co` + publishable key `sb_publishable_CC750vohuR5HcENzCKPNFw_AA6dqbi-`. Don't commit real values — this info is safe to duplicate into `.env.local` only.

## Stopped at
- Finished M0 bookkeeping. Next up: M1 — kill the fake bid.

## Next (first action of next session)
1. Open [SCHEMA.md](../SCHEMA.md), then this file.
2. Verify M0 by running `pnpm bootstrap && pnpm dev:all` fresh and hitting `basa.localhost:3000`. If fonts / images / tenant branding render correctly AND the 9 lots show sensible dollar prices (e.g. lot 1 Ligne Roset starts at $15, est $2,400), M0 is demo-passed.
3. If green, start M1:
   - Email Basta support with all 10 questions from [risks/basta-questions.md](../risks/basta-questions.md). Don't wait for reply.
   - Migration `step15_auctions_increment_table.sql`: add `auctions.bid_increment_table jsonb`.
   - Update `backend/src/routes/seller/publish.ts` to (a) persist the increment table to that column and (b) pass `allowedBidTypes: [MAX, NORMAL]` to `createItemForSale`.
   - Build backend `POST /api/basta/bid-support/:lotId` returning `{ saleId, itemId, allowedBidTypes, bid_increment_table, current_starting_bid_cents }`.
   - Rip the mock in `components/storefront/max-bid-section.tsx`. Wire to real `bidOnItem(type: MAX)` via Client API using the Bearer bidder token.
   - Verify in Basta admin that the bid lands.

---

## Update — M1 shipped same session

### Did
- Filled real env values into `.env.local` + `backend/.env` (Supabase publishable + service role, Basta account id + API key for `af9d77d6-2feb-4293-bd1d-dddc66b831d1`). `.gitignore` now allow-lists `.env.example` files.
- **Verified M0 demo gate.** `pnpm bootstrap` clean → `pnpm dev:all` → `Host: basa.localhost:3000` renders "Vintage Furniture Archive" with lot 1 Ligne Roset at `$15` / est `$2,400`. Committed as `226d5c8`.
- Fixed `lib/storefront-data.ts`: `status='published' AND scheduled_date >= now` filter hid BASA's currently-live, past-dated auction. Now accepts `status IN ('live','published')`.
- **M1 shipped.**
  - Re-pulled Basta Client API reference via `/basta` skill (error codes, bidOnItem union types, subscription shapes) → compiled into [`basta-integration.md`](../architecture/basta-integration.md) with a new "Bid flow (buyer MAX)" sequence diagram.
  - Migration `step15_auctions_increment_table` applied. Backfilled BASA's existing live sale so its M1 bid-support payload works without republish.
  - `backend/src/routes/seller/publish.ts` now persists `bid_increment_table` + `closing_time_countdown_ms` on the auctions row at publish, and passes `allowedBidTypes: [MAX, NORMAL]` to every `createItemForSale` call.
  - New backend route `GET /api/basta/bid-support/:lotId` returns the full payload the client needs to bid. Fixed a PostgREST FK-embedding ambiguity (`lots` ↔ `auctions` has two FKs).
  - New frontend module `lib/basta/`: `client.ts` (fetch-based `bidOnItem` wrapper + `bidErrorMessage` mapper for the documented error codes) and `bid-support.ts` (HTTP helper + `resolveIncrement` utility).
  - Rewrote `components/storefront/max-bid-section.tsx`: real three-step flow (bid-support → bidder-token → Client API bidOnItem), with idempotent retry on `INVALID_TOKEN` / `UNAUTHORIZED`, loading state, error UI, and a friendly user-facing message.
- Drafted `docs/memory/risks/basta-support-email-draft.md` — the 10 open questions bundled as a sendable email.

### Works
- `pnpm typecheck` both workspaces clean.
- `pnpm dev:all` both servers up.
- `curl localhost:4000/api/basta/bid-support/<basa-lot-1-uuid>` returns the expected payload with BASA's real `saleId` / `itemId` / increment table.
- 404 on unknown lot uuid; 400 on non-uuid input (Fastify schema validation).

### Broken / unverified
- Have NOT yet placed a live MAX bid from the browser with a signed-in buyer — that requires a signed-in Supabase user on `basa.localhost:3000` and visual confirmation in the Basta admin dashboard. Verify first thing next session.
- No tests on the new bid-support route. The `resolveIncrement` utility has edge cases worth a unit test pass.
- `lib/storefront-data.ts`: the `live`/`published` ordering using alphabetical `status` sort is a small hack; revisit when we have a real status machine.

### Stopped at
- M1 code + memory committed (next commit below). Next session: manual browser bid test → then M2 (webhooks).

### Next (first action of next session)
1. Open [SCHEMA.md](../SCHEMA.md), then this file.
2. Send the Basta support email from `risks/basta-support-email-draft.md`. Don't wait.
3. **Verify M1 in the browser.** Sign in as a buyer on `basa.localhost:3000`, open any lot, type a max bid (e.g. $100), hit SET MAX BID. Expected: loading → success state "YOUR MAX BID: $100". Cross-check in the Basta admin dashboard that the bid arrived against `saleId 1d34c974b0-3117070000020001`. If BidPlacedError shows up, check the errorCode — `BID_TOO_LOW` likely means our increment table is out of range; anything else means an env/token issue.
4. If green, start M2 (webhooks are the source of truth):
   - Migrations `step13_webhook_events.sql` and `step14_bids.sql`.
   - Fastify route `POST /api/webhooks/basta` with raw-body + idempotency.
   - Handlers for `BidOnItem`, `SaleStatusChanged`, `ItemsStatusChanged`.
   - Expose ngrok URL in Basta dashboard.
5. Clean up the 3 orphaned "Cross Patch Denim Jacket" lots on BASA's auction (sort_order 6-8) when convenient.

---

## Update — M2 shipped same session

### Did
- Cross-checked all 10 open Basta questions against the full skill reference (`management_api.md` + `client_api.md` + `webhooks.md` + `glossary.md`). 9 of 10 genuinely unanswered by public docs; Q7 downgraded (webhook payload has `userId` + `newLeader`/`prevLeader`, so the bid feed ships without needing a subscription-level identity field). Tightened the support email to cite which reference files were checked.
- M1 committed as `2030a2a` (code + memory together).
- **M2 shipped.**
  - Migration `step13_webhook_events` applied: PK=idempotency_key, payload jsonb, received_at/processed_at, error, denormalized basta_sale_id/basta_item_id. RLS enabled (service-role only).
  - Migration `step14_bids` applied: one row per Basta bidId, reactive flag, FK to auth.users with ON DELETE SET NULL, public read for published+ auctions, added to `supabase_realtime` publication.
  - Migration `step16_lots_live_status_closing` applied: expanded the `lots_live_status_check` CHECK constraint to include `closing` and `closed`. Caught via smoke test — the original constraint only allowed `{upcoming, live, sold, passed}`.
  - Backend route `POST /api/webhooks/basta` ([`backend/src/routes/webhooks/basta.ts`](../../../backend/src/routes/webhooks/basta.ts)): optional shared-secret gate (`x-basta-secret` header, `BASTA_WEBHOOK_SECRET` env), payload shape validation, idempotency via PK conflict → 200 `{duplicate}`, dispatch → stamp processed_at or error.
  - Three handlers under `backend/src/lib/webhook-handlers/`:
    - `bid-on-item.ts` — resolves lot/auction/tenant via `lots.basta_item_id`, upserts top-level bid + any reactive bids on `basta_bid_id`. Normalizes Basta's "2026-04-21 18:00:00.000000" timestamps to ISO-8601 UTC.
    - `sale-status.ts` — UNPUBLISHED→draft, PUBLISHED→published, OPEN→live, CLOSING→live, CLOSED→ended. Stamps `went_live_at` on first live, `ended_at` on close.
    - `items-status.ts` — UNPUBLISHED/PUBLISHED→upcoming, OPEN→live, CLOSING→closing, CLOSED→closed. sold/passed remain console-driven in M4.
  - Smoke-tested all four paths against the running backend with real BASA `saleId 1d34c974b0-3117070000020001` + real auth user `2f8aaaaa-dbc4-4f3d-9556-161dcb68adbb`: BidOnItem → persisted; duplicate replay → `{status:"duplicate"}`; SaleStatusChanged OPEN → auctions.status flipped; ItemsStatusChanged CLOSING → lots.live_status='closing'; malformed body → 400.
- Updated [basta-integration.md](../architecture/basta-integration.md) with a Webhook-flow ASCII diagram + flipped the M2 checkboxes. Last-verified date bumped.

### Works
- `pnpm typecheck` clean both workspaces.
- Webhook endpoint handles all three event types + duplicates + malformed payloads.
- Webhook events written to `webhook_events` with audit trail (error column populated on failure).
- Bids land in `bids` table; on the realtime publication so M3 frontend can subscribe.

### Broken / unverified
- Never tested against a real Basta delivery — ngrok URL hasn't been registered in the Basta dashboard yet. Do that next session + place a real bid via M1 UI; watch the webhook arrive and the bid land in `public.bids`.
- **Webhook signature verification is a stopgap** (`BASTA_WEBHOOK_SECRET` shared-secret header). Replace with Basta's real signature spec once support replies to Q5.
- `BASTA_WEBHOOK_SECRET` is NOT set in `backend/.env` yet — set it before exposing ngrok publicly, and configure the same header in Basta's webhook settings.
- No tests yet on the webhook route or any handler. The `normalizePlacedAt` parsing logic especially merits a unit test.
- BASA's 3 orphaned "Cross Patch Denim Jacket" lots (sort_order 6-8) still present.

### Stopped at
- M2 code + memory ready to commit. Next: commit, then live-fire against Basta via ngrok.

### Next (first action of next session)
1. Open [SCHEMA.md](../SCHEMA.md), then this file.
2. **Live-fire M2**: start `pnpm dev:all`, run `ngrok http 4000`, paste the `https://<slug>.ngrok.app/api/webhooks/basta` URL into the Basta dashboard's webhook settings, set the same `BASTA_WEBHOOK_SECRET` in both `backend/.env` and Basta's header config. Then place a test bid via the M1 buyer UI; tail `select * from webhook_events order by received_at desc limit 5` and verify `processed_at IS NOT NULL` + a corresponding row in `bids`.
3. Send the Basta support email.
4. Verify M1 in the browser (still not done — same as previous "next" list).
5. Start **M3**: live buyer screen with WebSocket subscription + Supabase Realtime on `bids`. `lib/basta/ws.ts` (graphql-ws wrapper), `useSaleActivity` + `useBidFeed` hooks, `app/auctions/[auctionId]/live/page.tsx` from the mobile Figma frames. NORMAL bid type wired on the one-tap BID button. See the M3 spec in `.claude/plans/fancy-knitting-bunny.md`.
